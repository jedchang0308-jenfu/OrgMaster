# DEV-057：身分與權限發布契約邊界

- 狀態：既有 v1 producer `RD Complete / Local QA-QC Passed`；B 的 `#principal-producer-impact` 已有本機 024／025 實作與隔離 PostgreSQL 證據，仍為 `RD Implementation In Progress`；AI-PDM v2 consumer 已有本機實作，跨 owner readback 與 Production L4 `NOT_RUN`
- 日期：2026-09-23；2026-09-24 principal-first amendment
- Native owner：`ORGMASTER/DEV-057#identity-grants`
- 來源：`JENFU/DEV-015#identity-grants`；同群目標任務 `AIPDM/DEV-121#target-authorization`
- 主責決策：`JENFU/DEV-015` 的 ADR-005；跨 repo 文件以固定 Project Key 與 native DEV 追溯，不依賴工作樹相對路徑。
- Spec Impact：與 [DEV-055](DEV-055-current-projection-contract.md)、[DEV-056](DEV-056-orgmaster-system-permission-catalog-sync.md) 的目前 producer 行為為 `Compatible exception`；保留 v1 欄位／ACL，但以 forward-only migration 修正 active-principal legacy 分支的 OrgMaster-role 依賴、AI-PDM Portal visibility 對 `orgmaster_authority` 的錯誤依賴，並補足 managed bind／legacy governance publish 共用的資料庫 writer fence 與永久 pair reservation。已套用 013／014／020 保持不變；producer contract 與 release profile 同步更新。

**2026-09-25 退役方向補充。** 使用者確認舊identity bridge無永久保留必要。此owner仍保留provider alias→canonical principal的必要來源契約及歷史證據；下游AI-PDM已切`principal_active`後只使用v2 handoff／principal session，不以v1或UID bridge維持授權。v1欄位、舊caller EXECUTE及相容projection只在尚有明列consumer使用的切換期保留，完成來源／caller readback與原O04～O06驗收後由各owner以forward-only方式撤出正常runtime；歷史mapping／receipt不得當allow fallback。這不要求刪除已套用migration或回寫歷史。

**AI-PDM authority 終態。** `legacy_authority` 僅供已列帳號過渡，不能以 principal-keyed local ACL 的形式永久保留第二套角色權威。OrgMaster 應以既有 assignment／catalog 發布流程承接逐 principal 核准的有效角色、scope、deny 與其版本；缺少等價能力時保留拒絕並列出差異，不依 email／employee 共用或角色名稱猜測授權。每個 `(employee, ai-pdm)` 的切換仍只由本 owner 的 v2 command 原子寫 authority／receipt／outbox，Platform 只處理自己的撤銷。全部有效人類帳號完成比對與切換後，撤除舊 switch caller 和 v1 alias grant 的正常 SELECT／EXECUTE；provider alias 的身份證明及歷史 receipt 保留，但不再作 grant 展開或 allow fallback。

<a id="architecture-final"></a>

## 架構定案：已定案（2026-09-24；文件交接）

`ORGMASTER/DEV-057#principal-producer-impact` 的現行交接由本節、[資料／writer 契約](#principal-implementation-contract)及[唯一 authority 命令](#principal-owner-command-amendment)共同構成；主責為 JENFU/DEV-015 的 `architecture-final`。原 v1 實作與證據保留，不能算成 B 的 Production 完成；024／025 是同一既有子任務的本機 producer 實作，沒有新增主任務。

**2026-09-25 本機 producer 進度。** 025 已建立來源受控的 `v_ai_pdm_principal_effective_grants_v2` 定義及 manifest，grant 從原治理、catalog、authority 與 typed admission 直接計算，不以 v1 alias grant view 作資料來源；同 principal 多個有效 alias 僅產生一組 grant。D57-17 已在 disposable PostgreSQL 以兩 alias／一 grant 驗證欄位、來源與 AI-PDM runtime／migrator 精確讀取；001–025 的 DEV-057 案例 17／17、owner migration bundle／runner 聚焦 21／21 PASS。AI-PDM consumer 已於本機改讀 v2，正式套用／跨 owner readback／切流與 v1 退役尚未執行。本段更新先前「僅文件／未實作」狀態，不改寫歷史證據。

**2026-09-25 Production baseline 修正。** 唯讀查核 sealed `RELEASED` receipt `ORGMASTER-REL-20260923182317791-BDD382A` 的 migration bundle 確認目前正式基線只有 001–021；022／023 雖在來源中，尚未正式套用。第一次 024／025 發布預檢因此以 `DEV057_PRINCIPAL_CONTRACT_APPEND_INVALID` 停止，沒有執行 migration 或切流。現行受控發布必須驗證 001–021 前綴逐筆不變，僅追加 022／023／024／025 的固定版本、路徑、source 與 applied hash，預期 `ledgerCount=25`、`applied=0..4`、`replayed=25-applied`；source-bound infra receipt、Job image、runtime 與完整 release gate 維持原要求。不能把來源已包含 022／023 視為 Production ledger 已套用。

- **投影依賴固定**：role-neutral `v_active_principal_mappings_v1` → `v_active_principal_accounts_v1` → grant／Portal／app admission 各自判定；mapping／typed 層不得反向讀 effective grants 或 Portal view。typed 層集中 managed／legacy 分類與 lifecycle 規則；下游只核對 exact pair／principal／employee／type，另加自己的 app、role、scope、時間條件，不能再複製一份 managed eligibility。
- **版本與雜湊固定**：沿 mapping 與 typed 的原同源 `mapping_version/published_at`；authorityVersion、assignmentVersion、Directory observation 版本不可跨域比較。`alias_set_hash` 使用所有 resolved 歷史 aliases 的 `[principal_id,employee_id,account_type,principal_issuer,principal_subject]` tuple，按 UTF-8 位元組序排序後以 canonical JSON／SHA-256 計算。欄位位置對應 Platform identity_issuer／identity_subject，無不同演算法。inactive 仍在集合；active/status、各 source revision 與時間只進 `source_snapshot_hash`，不改集合 hash。不得用穩定 hash 代替 current admission，或過濾 unresolved 歷史後宣稱完整。
- **唯一 command 與服務 guard 固定**：UI 與兩種 operator 呼叫同一 switch v2；server 依原權限及 step-up 驗證 actor，DB owner function 驗 exact target、CAS、來源版本、operation 綁定及原子資料不變條件。前後端自述角色不能替代 guard；不另建跨專案授權引擎。Platform 只持自己的撤銷交易，claim／ack 保留原 event ID，Platform invalidation 保留原 operation ID。
- **readback／錯誤固定**：command 零部分提交；400 參數形狀不符，403 actor 無權或 target 不獲 admission，409 CAS／source drift／operation conflict／legacy_receipt_unbound，503 producer 歧義／不可用或重試耗盡。readback 無 operation 回零列，絕非成功；有 receipt＋pending outbox 表示切換已提交、refresh 未完成，不能重做切換。provider/DB transport 成功不等於 authority 或 refresh 成功。
- **實際 consumer ACL 固定**：現行 023 的 typed view SELECT 清單沒有 `jenfu_ai_pdm_migrator`，不能把 runtime 可讀當成 065 owner function／cutover 可讀。024 僅對既有 `jenfu_ai_pdm_migrator` 增加 `orgmaster_contract` USAGE 與四個精確 view SELECT：`v_active_principal_mappings_v1`、`v_active_principal_accounts_v1`、`v_ai_pdm_entitlement_authority_v1`、`v_ai_pdm_effective_role_assignments_v1`。供 AI-PDM owner prepare／source 重驗／provision helper 使用；不給 alias-history 全表、switch EXECUTE 或任何 core DML。原 consumer 權利保留，manifest 明列這項 additive ACL；O05 舊 ACL 等價案例對這四項使用明確 expected delta，其他 ACL 仍須相等。不新增 IAM identity、CONNECT 或共享 login。
- **相容出口固定**：先讓新 command／readback 與 caller 可用，才各 owner 收斂自己的舊入口 EXECUTE；不提前撤仍使用中的 caller、不將 owner migration 管理權當 runtime fallback。原 O01～O07 補 typed 依賴無循環、同 aliases 狀態／發布更新 hash 不變、別名歧義仍拒絕、ack 失敗重播與舊 caller 退出；沿現有 test/QC 指令，不增加完成分母。

已對照目前 HEAD `f9a69c5f3f936a99ca3fbc18004e0a67f44a16b7` 的 migration 020／021／023、store／dispatcher／兩種 runner 與 tests；這是 source 審查，不是正式 relation/ACL readback。RD 可以調整私有實作與未套用 ordinal，不能另選 principal owner、分類來源、authority writer、交易終態或恢復方向。已列現況差異是待實作項；只有新證據要求改變契約時回寫同一 DEV。狀態為 `RD Implementation Ready／架構已定案／本機實作中`，real-provider／recovery／Production L4 仍待執行。

<a id="principal-producer-impact"></a>

## 子任務：principal producer 影響評估（2026-09-24；B 已定案）

`ORGMASTER/DEV-057#principal-producer-impact` 承接 `JENFU/DEV-015#principal-identity-reassessment`，與 `AIPDM/DEV-121#principal-consumer-impact` 對照。使用者已選 B：canonical `principal_id` 是跨系統唯一 security subject。本節定案 OrgMaster producer 的目標契約，成熟度為 `Architecture Finalized / RD Implementation Ready`；下方 v1 producer 實作／驗證仍是當時證據，不表示 B 已實作或發布。

- 查核現行 managed `principal-managed:*` 與 legacy principal 的發行來源、唯一 writer、有效 `(issuer, subject)→principal_id→employee_id` 及 account classification；Provider UID 只能是已驗證來源 alias，email 不作 identity fallback。`employee_id` 保持 OrgMaster domain identifier。
- B 的正常登入沿用按 pair 查詢的 `v_active_principal_mappings_v1` 與 typed adapter；producer 負責跨 pair 不變條件，consumer 不以全表掃描或 employee 列數判斷 identity。缺失／重複映射仍 fail closed，不能讀 sibling core 或默默改 v1 欄位／ACL。
- 一般與特權 principal 分離，Platform 擁有 principal-wide 撤銷；本切片不新增多 provider 綁定 UI／自動 merge。若歷史資料已有多 alias，須在 producer readback 證明一致性；不能為湊成單列刪除或合併資料。
- 回送主責 DEV 可查的 owner 影響、相容／回復及負向驗收；本文件定案不把 B 算成 Producer 已上線或 Production L4 通過。

### B 的 producer 不變條件與版本邊界

1. OrgMaster 仍是 `principal_id` 的唯一發布者。保留已發布 legacy ID；managed principal 沿用現有 `principal-managed:*` owner writer。Platform／AI-PDM 只消費版本化 `orgmaster_contract`，不得以 Firebase UID、email、員工編號、AI-PDM `pdm:*` 或 local user ID 自行產生 canonical principal；`employee_id` 僅為本系統 domain ID。
2. 每個 active `(issuer, subject)` 必須恰指向一個 principal、employee、account class；同一 principal 的所有 aliases 維持相同 employee／account class，此所屬關係不隨 alias 變動，也不得停用後回收 principal 給另一人。**同一 employee 可有多 principal，同一 principal 可有多 pair；這兩種合法多列不是 duplicate pair。** 日常與特權 principal 不合併 role。owner writer／reservation 保持 pair 及 principal 所屬的不可重派，consumer 按 pair 查 mapping，再按 pair＋principal 查 typed row；零筆／同鍵多筆／分類衝突拒絕，不以 `DISTINCT`、`LIMIT 1` 或來源優先序掩蓋。alias 整體一致性由 producer 不變條件與 readback 證明，不要求每個 request 掃所有 aliases。
3. 現有 role-neutral `v_active_principal_mappings_v1` 與 typed `v_active_principal_accounts_v1` 足以定義正常 pair admission 的讀取形狀；保留欄位、owner、ACL 與 v1 語意，不為改名再包一層 identity service。補正 writer／reservation 走 owner-owned forward-only migration。024的reservation／writer與新增alias-history contract已固定實作方式；只有超出本契約的實證矛盾才修訂同一DEV；不改寫已套用 013／021／023 等 migration，不讓 consumer 直讀 `orgmaster_core`。
4. Authority 的 `(employee, application)` key **只選業務來源**，不是 security subject。沿用 migration 020 中 employee grant 限 `human_personal`、privileged grant 限 exact `targetPrincipalId`／`human_privileged` 的規則；AI-PDM 取 employee 選來源後仍須查 exact principal 的有效 grants。不能為 B 將同員工全部 grants 複製給每個 principal，也不新增 principal authority 資料副本。OrgMaster 不替 AI-PDM 決定 route allow 或 local resource ownership。
5. Platform 負責 principal epoch、v1 pair epoch 及全域登出；本 repo 的 target session 必須明確記錄採用的 epoch key。Wire v1／v2 與 app session version 不同；v1 proof 若要進 principal session，依 `JENFU/DEV-015` 同時驗 pair proof 與 principal revocation barrier，不能直接用 current epoch 蓋過舊 proof。相容期不新增／轉移 alias，以維持 Platform 已保留 pair 的撤銷覆蓋；binding 停用仍立即使 protected request 拒絕，不能等待 outbox 才撤權。

實作前讀回目前 managed／legacy pair、principal／employee／account class、reservation、active governance 及 contract manifest 的 source-bound count 與 key；任何一對多、空值、分類缺失或停用 identity 都不得自動 merge／補值。隔離負向案例至少涵蓋兩 pair 同 principal 同 employee／class、同 principal 跨 employee、特權與日常 principal、pair 競態、managed lifecycle pending／deactivated、contract 不可用與 publisher replay。完成這些及 v1/v2 consumer conformance 後才能認列 B producer ready；DEV-014 現存 compatibility projection／receipt 缺口另按其 owner 契約處理，不能靠重跑相同 switch 掩蓋。

<a id="principal-implementation-contract"></a>

### B 實作定案：registry、alias history、writer 與 target session

文件成熟度為 `Architecture Finalized / RD Implementation Ready`。以 HEAD `f9a69c5f3f936a99ca3fbc18004e0a67f44a16b7` 的 migration 023 為基線；`db/migrations/024_dev057_principal_identity_invariants.sql` 的reservations、alias history、legacy／managed writer fence及authority v2命令／operation readback已有disposable PostgreSQL D57-01～15驗證。本人UI與DEV-013單員工／DEV-014雙fixture operator的v2接線通過本機目標測試；024已讓有效角色與Portal入口共用typed principal投影，未分類或同pair多筆分類的雙入口拒絕、唯一已分類pair維持授權通過D57-07；source-bound owner release profile／runner已納入024，相關48/48測試與DB boundary PASS。operator真實PG端到端、target session行為與受控環境套用仍待完成。後續 ledger 占號只調整未套用 ordinal／引用；保留 001–023 原內容。以下契約依本輪ownership／command修訂固定；舊入口ACL cleanup在caller遷移後另追加owner forward-only migration，不與相容DDL同時撤權。

| Owner 物件 | 固定資料與責任 |
| --- | --- |
| 新 `orgmaster_core.principal_ownership_reservations` | `principal_id text PK`、`employee_id text NOT NULL`、`account_type text NOT NULL`（沿用 typed contract enum；本次人類路徑只接受 `human_personal/human_privileged`）、`first_seen_at timestamptz`、`source_revision text`。principal／employee／type 永久保留，不轉派、不因刪帳釋放ID；索引 `(employee_id,principal_id)`。不是 app grant registry。 |
| 既有 `principal_identity_reservations` additive 欄位 | 新 `principal_id text NULL FK` 指上述表。保留 `(principal_issuer,principal_subject)` 唯一鍵／原歷史欄位；舊歷史不能證明 principal 的列維持 reserved＋NULL，不能被新 writer 重用或當 active admission。已知 principal 欄位只允許首次 null→verified value，不可修改為另一principal。runtime無直接 UPDATE／DELETE 權。 |
| 新 `orgmaster_contract.v_principal_alias_history_v1` | 一列一個reserved pair：`contract_version='orgmaster.principal-alias-history.v1'`、`principal_issuer,principal_subject,principal_id,employee_id,account_type,first_seen_at,source_revision`；`resolution_status=resolved|unresolved`，`active boolean`。inactive保留、unresolved欄位明示NULL且不得初始化consumer state。只公布身分與歷史歸屬，無email、token、角色；供Platform初始化／撤銷覆蓋及owner readback。 |
| 現有 `v_active_principal_mappings_v1`／`v_active_principal_accounts_v1` | 欄位與語意保持；型別欄實名為 `account_type`，各 app 可在程式叫 `accountClass`，不可新增另一分類權威。writer保證principal一致性；views對遇到的duplicate仍保留而不是 `DISTINCT`。existing lifecycle／Directory／employee／published admission條件完整保留。 |
| `orgmaster_core.app_sessions` additive 欄位 | `session_schema_version smallint DEFAULT 1`、`epoch_kind text DEFAULT 'provider_pair'`、`principal_auth_epoch bigint NULL`；CHECK與Platform一致：v1/pair/null或v2/principal/0..safeIntMax。原pair epoch仍留相容證據；v2保存原authenticatedAt，cookie仍opaque token。 |

新 history view使用既有 `jenfu_orgmaster_migrator` owner、PUBLIC拒絕；只授Platform migrator/runtime讀取及OrgMaster既有owner查核身份；principal-indexed查詢必須保留inactive aliases，unresolved reservation若無法排除屬於該principal／employee，不能只過濾NULL後宣稱alias集合完整；加入producer/consumer manifest。AI-PDM runtime只需active mapping／typed與grant契約，不增加讀全alias history的權限。欄位hash與manifest更新是待實作產物，本輪不編造hash。跨schema只SELECT contract；沒有Platform或AI-PDM core FK，也沒有新IAM／Secret。

**Writer 定案。** 沿用migration021鎖順序 `managed_identity_admission_authority(singleton) → persistence_authority → principal/pair reservations按鍵排序`。legacy governance publish、managed bind／verify與reactivation全部在同一transaction呼叫owner invariant helper：擷取proposed pair＋principal＋employee＋typed account→對照歷史reservation→新增首次reservation→完成原publication／admission。對一principal跨employee/type、pair改principal或既有unresolved reservation一律拒絕；不能按employee擇第一筆。singleton序列化所有可能改active identity的writer，正常登入只讀，不取得全域writer lock。

024對舊base writer的直接EXECUTE與managed identity直接DML做最小收斂：runtime只能用既有受控owner函式，managed／legacy writer內部皆呼叫同一invariant檢查；migration／owner函式是唯一可寫reservation的身份。不得靠HTTP caller傳入「已驗證」旗標略過檢查。授權／typed classification變更同樣受此不變條件；principal account type固定，變更日常／特權身分需既有治理流程給不同principal，不能原地改class。employee status停用與Directory missing可立即阻斷admission；恢復沿DEV-014 lifecycle完成Platform invalidation receipt後才放行，不能舊cookie復活。

024先在相同lock下用existing published治理歷史、managed identity紀錄與reservation做結構一致性preflight；owner readback operation完成已知歷史歸屬回填。不能從current email或同employee推測過去principal。已知同pair存在互斥principal的active來源→整筆transaction abort；僅歷史unresolved列保留quarantine，不阻斷完全不相關principal，也不可宣告該principal完成。第一版不另建principal註冊UI，既有managed發行器及legacy publisher便是寫入入口；新alias綁至已有principal的功能在v1相容期停用，原alias停用不受阻。

**Target session 定案。** `orgmasterSsoHandoff.ts`先依wire精確parser，於同一 REPEATABLE READ READ ONLY snapshot 核對 exact pair/principal/employee/type與OrgMaster專用app admission；v1驗pair epoch＋原auth time／principal barrier、v2驗source principal stamp。均成功後新增schema2 row，記 `epoch_kind=principal` 與新principal epoch；新token不沿用舊session ID。所有protected request及既有direct/managed-login入口都由`orgmasterAuthApi.ts`同一verified context取得principal，按schema選reader；v2 state缺失／讀取失敗不走pair fallback。特權仍要求OrgMaster既有AAL2與app role，typed personal principal不因Platform登入成功變admin。protected request 可先驗 cookie/token 的固定證明；session row／revoke、Platform v3 state、active pair／typed 與本 app role/admission 必須在同一 REPEATABLE READ READ ONLY snapshot 驗證，不能先在另一連線驗 epoch 後只在 snapshot 讀角色。角色異動依該讀點判斷；employee ID 只作 domain 關聯。O06 補驗簽後、首個資料 read 前的 local/global logout，既有已開始請求可完成的限制不變。

相容期v1 row僅依原pair規則讀取；轉schema2須以原authenticatedAt重新admit，不更新原auth time。global logout依Platform新版principal/pair同步撤銷；OrgMaster只讀其v3 state，不取得Platform auth writer鎖，不在OrgMaster transaction持有writer lock時同步呼叫Platform撤銷；lifecycle沿既有outbox／receipt順序避免跨owner鎖循環；本地logout只撤OrgMaster row。回復baseline必須能讀schema2、用principal epoch且維持app admission；清空v2 sessions或改v1 emitter不允許回到忽略principal state的binary。history／reservation不刪、DDL不down。

| 精確實作面（repo-relative） | Owner責任／對應原驗收 |
| --- | --- |
| 新 `db/migrations/024_dev057_principal_identity_invariants.sql`、`db/migrations/README.md`、`scripts/lib/dev040-routine-release.mjs`、`config/release/dev040-orgmaster-independent-production-v3.json`及既有contract manifest | 上述DDL、writer EXECUTE／DML收斂、alias history與session additive欄位；O02／O05／O06。 |
| `server/orgmasterManagedIdentityRepository.ts`、`orgmasterManagedIdentityStore.ts`、`orgmasterPrincipalAdmissionRepository.ts`、`orgmasterPersistenceRepository.ts`及同名tests | proposed snapshot＋reservation不變條件；禁止兩個writer實作兩套主體推導；O01／O02。 |
| `server/orgmasterSsoHandoff.ts`、`orgmasterAuthApi.ts`、`orgmasterAuthEpochRepository.ts`、`orgmasterSessionRepository.ts`及既有tests；新 `contracts/jenfu-sso-handoff/v2/` | principal/pair epoch明確reader、所有login入口、v1/v2 conformance、opaque session rotation；O01／O06。 |
| `server/employeeAuthoritySwitchStore.ts`、`server/orgmasterEntitlementInvalidationDispatcher.ts`、`scripts/dev013-production-authority-switch-runner.mjs`、`scripts/lib/dev013-production-authority-switch.mjs`、`scripts/dev014-production-login-authority-switch-runner.mjs`及tests | 正常UI、DEV-013 operator、DEV-014 fixture runner全部使用本owner新`orgmaster_contract.switch_employee_entitlement_authority_v2`與versioned readback；exact pair／principal／employee查核，不按employee列數判歧義。CAS／receipt／outbox均在OrgMaster自有transaction；Platform只做invalidation，不新增Platform v3；O03／O04。 |
| `scripts/qc-dev-057-contract.mjs`、`scripts/qc-dev-047-postgres.mjs --suite=dev057`及既有fixture helper | O02已補跨employee回填衝突、跨class、same employee多principal、two aliases同principal、unresolved reuse及舊writer競態；O05已補history resolved／unresolved、ACL與永久reservation拒絕改寫，disposable PostgreSQL PASS。reservation replay全向量與O06兩session schema／撤銷／rollback仍待實作及驗證。 |

操作receipt記source revision、contract hashes、active/reserved/resolved/unresolved counts、按principal排序的alias-set hash與operation ID；不含credentials。同operation同input hash重播只回readback；來源revision/hash變更先重做preflight，不把「已連結」UI當contract成立。managed mapping缺失時分辨published revision、Directory、pending lifecycle及typed row；修owner來源後才能重試，不能複製到legacy投影湊數。這關閉B文件中的formal readback設計問題，**不宣稱**DEV-014現場缺口已修正。

進入順序：024 producer相容contract→Platform009 principal state→本repo雙版session reader→按本client readiness啟用wire；與AI-PDM可獨立驗證／release，無永久三app cohort。沿用O01～O07、`test:dev-013`／`test:dev-049`／`test:dev-050`、`qc:dev-057:contract`／`qc:dev-057:postgres`／`check:db-boundary`。Production值、source lock及readback是未取得的執行證據，不是留待RD重新決定的架構。目前024的reservation／history／writer fence、session DDL及authority v2命令／readback切片通過本機QC；本人UI與兩個operator已完成本機接線，operator真實PG端到端及target session尚未完成，不能視為producer或target上線。

<a id="principal-review-20260924"></a>

### 2026-09-24 RD 複審補正

沿JENFU/DEV-015同名複審：O02／O05加入unresolved alias不可因NULL被過濾而冒充complete、P1兩alias的history readback；O06驗global logout後所有aliases失效。Platform auth-state writer鎖僅由Platform持有，OrgMaster正常admission只讀contract，lifecycle用outbox／receipt，禁止跨owner同步鎖循環。原type／app-role判斷及writer不變條件保持，文件不代表上述PG／provider測試已執行。

<a id="principal-owner-command-amendment"></a>

### 2026-09-24 ownership／command 修訂：唯一authority writer與身分事實

**更正來源。** 本repo migration010把`access_governance`原tables及相關functions移交OrgMaster，舊schema names為相容層；020直接從`orgmaster_core.application_authority_state／employee_authority_overrides`讀取。前輪本DEV的「Platform-owned CAS／projection」敘述與此不符，已修正。保留applied migrations原文與歷史evidence，不以新文件宣稱Production owner／ACL已讀回。

**Authority命令。** 024新增`orgmaster_contract.switch_employee_entitlement_authority_v2`：參數依序為`p_application_id text,p_employee_id text,p_to_authority_source text,p_expected_authority_version bigint,p_operation_id text,p_batch_id text,p_assignment_version_id text,p_actor text,p_reason text,p_principal_id text,p_identity_issuer text,p_identity_subject text`。回傳沿既有shape：`receipt_id uuid,authority_version bigint,outbox_event_id uuid,session_refresh_state text,replayed boolean`。限現有ai-pdm employee authority；後三欄為**target**身分，不能與操作人actor混用。每個public UI請求先經既有verified actor、step-up與system-admin guard；operator沿既有owner machine identity及受控operation，不以browser自述actor或任意employee清單取代授權。

同 transaction 固定 operation key → 既有 managed_admission／persistence singleton → application／employee override → receipt／outbox 鎖順序。新 switch v2 為 VOLATILE，只接受 READ COMMITTED write transaction；在取鎖前檢查 isolation，RR／SERIALIZABLE caller 明確回 `authority_transaction_mode_invalid`（server 503、零 mutation），由 caller 重開正確 transaction。這取代前輪「SERIALIZABLE 遇競態重試即可保證鎖後 current」的描述；鎖本身不刷新 snapshot，也不能假設 producer writer 都在 SSI 中。operation 鎖後以新的 SQL 先查成功 receipt；同 key 不同 target/hash 回409，同 input 成功則返回已提交結果，不重新要求 target admission 或新的 CAS。只有未提交的 operation 才取其餘鎖，**全部 owner locks 取得後**用下一個 SQL 讀點驗 exact target pair／principal／employee／human_personal、assignment version、scope 及 CAS。writer 在這些 owner locks 下發布，不能用 HTTP 預檢或等鎖前讀值代替。

fresh operation 將 authority、receipt、outbox 一次 commit，無 HTTP、Platform writer 或 GCS 在交易內。批次 fixture 先取全部排序 operation locks，再取 singleton／排序 target locks；每個 employee/application 使用獨立 operation ID，batch ID 才共用，任何一筆失敗全 rollback。deadlock／timeout 回滾後由新 transaction readback，不能只重跑失敗 statement；新命令與舊相容轉呼叫遵守同一 isolation／鎖順序。readback 函式是純讀，不受 writer isolation 限制。

024在既有`orgmaster_core.authority_switch_receipts`只追加`request_contract_version text,request_hash char(64),target_principal_id text,target_identity_issuer text,target_identity_subject text` nullable欄；新v2列五欄必填，舊列NULL且不回填猜測。hash為上述十二參數含expected version、明確null及contract version的canonical SHA-256；原unique(operation_id,application_id,employee_id)與receipt ID保持。新增`orgmaster_contract.read_employee_authority_operation_v1(p_application_id text,p_employee_id text,p_operation_id text)`回0或1列`receipt jsonb,outbox jsonb`，receipt明列原ID／operation／target／from-to／authority version／batch／assignment／actor／reason／switched_at、新request綁定及refresh state／platform receipt id；outbox僅列event／operation／employee／application ID、kind、actor、reason code、created/completed time、status／attempt count及platform receipt id，以供受控operator核對同一事件，不出token或credential。此為operator readback，不是另一份狀態表。

新命令與readback以OrgMaster owner SECURITY DEFINER、固定pg_catalog search_path及全限定名稱；PUBLIC、AI-PDM、Platform runtime無EXECUTE。限既有OrgMaster runtime（通過原UI guard）及migrator；相容期只另授Platform migrator呼叫新命令／readback供Platform舊v2薄轉呼叫，不授任何OrgMaster table DML。舊receipt request_hash為NULL時，新命令回`legacy_receipt_unbound`並指向readback，不把它升格成新input、不重做side effect；authorized readback可顯示歷史已提交結果。同hash重播回原commit結果與另列current refresh狀態，不用已改變的active binding拒絕已完成receipt查證。

**切換與refresh兩個終態。** authority／receipt commit後即以新authorityVersion提供target讀取，receipt refresh=pending不代表切換失敗。既有worker依`claim_entitlement_change_outbox_v1`取得event，釋放claim transaction後呼叫Platform版本化invalidation；dispatcher沿用event.operation_id傳入Platform；Platform以operation_id原子去重並寫epoch／receipt，event_id仍僅供OrgMaster claim／ack。DEV-014 lifecycle另有自己的event ID去重契約，不改此既有對應。成功後worker經`complete_entitlement_change_outbox_v1`回ack。ack失敗重試原event，不重增epoch或再切authority；未知commit先readback。只有ack完整才宣告refresh完成。rollback是expected current version＋新operation的反向切換，authority version持續增加、另生event，不能刪舊receipt／取消已完成invalidation或使舊cookie復活。

**有限相容與cleanup。** 024先加新命令、typed依賴與readback；OrgMaster新UI／兩operator callers、Platform009薄轉呼叫各自發布。一般caller全部遷移且pending/replay有新reader後，OrgMaster追加forward-only cleanup，只撤銷本owner historical `access_governance.switch_employee_entitlement_authority_v1(text,text,text,bigint,text,text,text,text,text)` 對runtime／一般caller的EXECUTE（包括PUBLIC及實際繼承路徑），不改歷史body或資料。這是OrgMaster自有compatibility routine精確ACL退場，須在boundary manifest明列，不允許其他legacy schema DDL。trusted owner/migrator保留管理能力不代表舊operator package仍可dispatch；source-bound runner只接受新命令。Platform自己的舊v2 consumer歸零後由Platform撤銷其舊入口caller EXECUTE，再收斂OrgMaster給Platform migrator的temporary delegation。cleanup按各owner ledger編號，before/after ACL與所有caller清單是原O04／O05 readback；不能在相容migration先撤仍使用中的入口。rollback baseline必須是新caller＋新命令版本，不能退回需要已撤入口的binary。不以服務同時發布或搬回資料解決此邊界。

**共同身分事實。** 不新增身分服務、materialized cache或平行registry。`v_active_principal_mappings_v1`維持role-neutral pair admission根；`v_active_principal_accounts_v1`為其唯一typed分類／managed lifecycle衍生。024讓effective grants與Portal view以exact tuple引用該typed結果，移除各自重複的`governance_account_types`及managed→human_personal推導。public v1欄位與版本命名空間保持，grant assignment/scope/time、Portal入口指派及OrgMaster app-role admission各自保留；不能把app role納入共同identity根，也不能把typed failure降級成未分類allow。same principal多aliases須維持相同employee/type，不因join重複產生多份grant或提高priority；mapping歧義仍可見並拒絕，不用DISTINCT掩蓋。涉及必要語意收緊時記expected before/after，原有效合法結果須等價。

**principal grant 終態，取代 alias 展開。** 024 的 `v_ai_pdm_effective_role_assignments_v1` 與 consumer 的逐 alias 對等檢查只是切換期契約；它不應成為 B 架構的永久授權讀取器。OrgMaster 後續以新增 forward-only migration 發布版本化 `orgmaster_contract.v_ai_pdm_principal_effective_grants_v2`：一筆有效 grant 由 `(principal_id, employee_id, application_id, assignment_id, grant_kind, delegation_id, role_id, scope_kind, scope_key, authority_version)` 識別，不含 issuer／subject 或 UID，保留原 assignment／catalog／時間／subject-type 條件。來源必須先證明同一 principal 的每個 active alias 對應唯一相同 employee／account type、沒有 unresolved reservation 或重複 pair；歧義拒絕，不以 `DISTINCT` 或 `GROUP BY` 掩蓋衝突。provider pair 只在 admission 驗證；grant 計算每個 principal 一次。AI-PDM 在同一已驗證 snapshot 讀此 v2 contract 和 selected authority，依 principal_id 授權；不再逐 alias join grants。先以真實資料 readback 比對 v1 每個合法 principal 的去重結果、缺列／額外列／衝突拒絕及授權決策，再切 consumer。consumer、Portal 及回復版本全數不依賴 v1 後，以 owner forward-only cleanup 撤銷舊 reader 權限並退役 v1 view；歷史資料與 receipt 保留但不再形成授權分支。這屬既有 DEV-057／DEV-121 子任務，不能新增永久 bridge 或藉切換擴大 grant。

**原案例與收斂出口。** O01／O02／O05補無OrgMaster角色的PDM身分、managed pending／Directory missing／分類衝突、兩aliases合法但grant不倍增，以及新命令零跨owner DML。O03／O04 補鎖等待期間 producer 先 commit → 後方新 SQL 見新 mapping/assignment 或拒絕、RR/SERIALIZABLE writer caller 零寫入拒絕，以及 UI 與 operators 同命令、同hash／異hash／legacy receipt readback、兩fixture原子切換、ACK中斷重播不重切、reverse CAS、舊caller cleanup後拒絕；A06驗Platform只增一次epoch。owner／schema／relation kind／function owner／實際ACL及manifest須readback，不用SQL註解證明所有權。

Architecture Finalized／RD Implementation Ready僅指上述修訂契約。DEV-049／050／055、DEV-014與既有DEV-057 local PASS保留原意；024基礎DDL、history readback、writer fence及authority v2命令／operation readback已在disposable PostgreSQL通過D57-01～15，本人UI與兩個operator目標測試通過。operator真實PG端到端、session整合、cleanup、real-provider及Production案例仍NOT_RUN。release executor共用列後續維護：先契約vectors，實際重複缺陷才考慮小型共用工具，不新增中央發布服務或本DEV完成分母。

## 目標與 owner 邊界

OrgMaster 是 active employee／principal、AI-PDM 授權來源與 published grants 的 producer。此 DEV 只定義 producer 可對外保證的唯一性、一致性與版本語意；不把 AI-PDM role catalog、route policy 或 Platform SSO broker 移入 OrgMaster，也不讓 consumer 讀 `orgmaster_core`。

現有 `orgmaster_contract.v_active_principal_mappings_v1`、`v_orgmaster_session_principals_v1`、`v_ai_pdm_entitlement_authority_v1`、`v_ai_pdm_effective_role_assignments_v1`、`v_portal_app_visibility_v1` 為起點。DEV-055／056 及 migrations 001–020 的既有實作／驗證保持原狀；修正只能追加 forward-only migration，不修改已套用 migration 或重寫歷史 evidence。`orgmaster_contract.v_active_principal_mappings_v1` 是唯一 identity producer；`orgmaster_contract.v_active_principal_accounts_v1` 是由 OrgMaster 擁有的 account-typed adapter；舊名 `access_governance.v_active_principal_links_v1` 是既有 OrgMaster-owned historical compatibility projection，本次不改寫其歷史definition，也不得把它當成managed principal的帳戶分類契約。

| Producer projection | 它回答的問題 | 不可代替 |
| --- | --- | --- |
| `orgmaster_contract.v_active_principal_mappings_v1` | issuer＋subject 是否唯一連到 active employee；**不要求任何 app role，也不回答 account type**。Platform／AI-PDM 身分 admission 與 managed-login adapter 以它為唯一 producer。 | Account classification、OrgMaster permission、Portal visibility、AI-PDM permission。 |
| `orgmaster_contract.v_active_principal_accounts_v1` | canonical mapping 是否另有可信 account classification。Managed row 須與 active managed identity 的 employee／principal／issuer／subject／admission revision／發布時間完全相符，且 admission 開啟、Directory observation 為 present、無未完成 lifecycle event；legacy row 須有 active governance `principalAdmission`。未知分類不輸出。 | Identity-only admission、app role、Portal visibility、AI-PDM permission。 |
| `access_governance.v_active_principal_links_v1` | OrgMaster-owned historical compatibility projection，既有 v1 consumer 相容使用。它不會因 OrgMaster migration 自動成為 managed-principal adapter；本次不改寫其歷史definition，不能因舊名誤認為Platform-owned。 | 新的 managed principal classification 與 authority-switch preflight。 |
| `orgmaster_contract.v_orgmaster_session_principals_v1` | active mapping 之上是否另有有效 OrgMaster app／global role；OrgMaster callback 使用它。v2 bootstrap view 可另外允許 AI-PDM assignment，但不會授予 OrgMaster app permission。 | 其他目標的身分或權限。 |
| `v_portal_app_visibility_v1` | 已發布、有效且匹配 active principal 的某 app **入口指派**是否讓 Portal 啟動；對 AI-PDM 不以 `authority_source` 分流。 | 目標業務 route allow；local ACL 不能自行產生入口。 |
| AI-PDM authority／effective-grant views | 目標的來源與 OrgMaster published grants。 | AI-PDM local resource／role／route policy。 |

authority-switch caller 必須以該 operation 已選定的 principal／provider pair 讀 `v_active_principal_mappings_v1` 及 `v_active_principal_accounts_v1`，兩個結果各恰一筆、issuer／subject／principal／employee 相同，且符合 operation 的 account class；既有 DEV-014 fixture 限 `human_personal`。不能把「整個 employee 只能一筆」擴成 B 的通用身分規則；既有窄 fixture runner 尚未改成多 principal 模式，不當作 B conformance。OrgMaster-owned新CAS function於transaction內重驗typed account，只寫OrgMaster authority／receipt／outbox；Platform contract僅用於後續invalidation。Role-neutral mapping 缺 account classification 時不進 typed adapter，禁止預設分類或把 mapping 當 app permission。

## 契約不變條件

1. 對一個 `(issuer, subject)`，接受的 active principal 解析結果必須**恰為一筆**；零表示未獲 admission，超過一筆是契約歧義並拒絕。現有 v1 view 以 `UNION ALL` 暴露 legacy 與 managed 來源；producer 不得用 `DISTINCT`、`LIMIT 1` 或來源優先序藏掉衝突。所有可建立或發布該 pair 的 writer 必須先取得同一 `managed_identity_admission_authority` singleton row lock；legacy governance writer 在 lock 內檢查並寫入 append-only `principal_identity_reservations`，managed bind／verify 在同一 lock 內檢查該 reservation。consumer 仍以零筆／多筆 fail closed。
2. AI-PDM 的 `(employee, application)` authority 必須恰為一個有效來源，且每筆 effective grant 的 `authorityVersion`、employee、application、principal 與 authority row 一致。`legacy_authority` 不得與 `orgmaster_authority` 合併或在 producer 故障時自動回退。
3. `mappingVersion` 是身分映射版本；`authorityVersion` 是應用授權來源版本；assignment／catalog 另有其版本。任何版本只在同一 authority＋object＋scope 比較，不提供跨域相等承諾。
4. 既有 v1 views 是定案 producer contract：`v_active_principal_mappings_v1` 保留衝突列；`v_ai_pdm_entitlement_authority_v1` 發布 `(application_id, employee_id)` 的唯一有效來源及版本；`v_ai_pdm_effective_role_assignments_v1` 僅在 `orgmaster_authority` 時發布 grants，且其列攜帶同一 `authority_version`。consumer 的純授權讀取用同一短暫 PostgreSQL `REPEATABLE READ READ ONLY` transaction；已有 owner write transaction 的命令使用其 transaction-bound evaluator，所有 view 與 session/epoch 讀取仍共用一份 snapshot，不另開旁路連線；OrgMaster 寫入側的 authority mutation 與 outbox 必須原子 commit。producer 不提供跨域版本相等、業務 route policy 或讀 `orgmaster_core` 的例外。
5. DEV-037 定義手動 workspace 指派 key `current`；現行 v3 producer 在 migration 020 直接發布 assignment `scope.value`，而 position-adoption source 條件要求 `company-jenfu`。兩者都是目前 producer 的有效來源語意，不能宣稱只有一個 key。OrgMaster 只發布 canonical grant，不替 AI-PDM 判定本地公司或資源歸屬；目標端先以可信 server-side membership／resource read 約束資料，再用 owner 明列的 company＋source-key 映射。若實際已發布 grant key 未被 consumer contract 覆蓋，owner 先讀回治理版本與投影並釐清，不讓 consumer 猜測或把 URL 參數當 project grant。
6. Portal AI-PDM visibility 是**入口指派**，不是 AI-PDM business entitlement。有效治理版本中的 AI-PDM 直接指派或有效委派，須沿用現行 effective-grant view 的 position 來源、catalog role／scope、subject type、app／role 狀態與時間判定，匹配 active employee／principal；唯獨不以 `authority_source` 篩掉 legacy 使用者。不能改為直接掃未驗證的 raw assignment JSON。`assignment_version` 仍取該 active governance `publishedVersions.versionNumber`，使 Platform code 發行／兌換只比較同一 app 的入口版本。若只有 AI-PDM local ACL、沒有 OrgMaster 已發布入口指派，Portal 不顯示；若只有入口指派、沒有目標權限，AI-PDM API 拒絕。OrgMaster 自身入口仍由自己的 app-specific role 判斷。

## RD 範圍與停止條件

- 架構審查已確認 migration 013 的 active-principal view 以 `UNION ALL` 保留兩來源列，但 legacy 候選仍額外要求 OrgMaster global role；migration 014 已另有 OrgMaster app-scoped session view；migration 020 的 effective-grant view 依 authority view 篩出 OrgMaster 權威，而 Portal AI-PDM visibility 又只從該 effective view 讀取，使 `legacy_authority` 使用者缺入口。另發現 current bind／verify 會鎖 `managed_identity_admission_authority`，但現行 `write_active_persistence_artifacts_with_identity_fence_v1` 只經 base writer 鎖 `persistence_authority`，沒有共同 fence；DEV-047 A9a 的原始要求未由目前組合實作維持。沿用 v1 欄位／ACL，以 migration 021 一次修正兩個 view 與 writer fence，不新增 v2 producer view。
- RD 實作時以 current source、view definition、writer、ACL 及 consumer manifest 建立固定 contract vectors；重現 legacy＋managed 競爭、員工停用／重新連結、governance publish、authority switch、outbox 未消費及 grant 有效期邊界。分別判定正確零筆、歧義及 producer 不可用，不能把後兩者當作未指派。
- Shared writer 必須採固定鎖順序：先鎖 `managed_identity_admission_authority(singleton=true)`，再呼叫會鎖 `persistence_authority` 的 owner writer；在同一 transaction 驗證 active governance version、拒絕重複 pair／reservation owner 漂移／managed pair 重疊，並為已發布 legacy identity pair 寫入 reservation。migration 套用前也以相同順序鎖兩列並做候選 projection preflight；既有資料有歧義即 rollback。
- role-neutral v1 是有意的身分接受範圍擴充；啟用前稽核 consumer manifest／code，若任何 consumer 把 active mapping 當作 OrgMaster app-role 證明，先修該 consumer或制定新版契約，不得直接開放其 app session。OrgMaster `v_orgmaster_session_principals_v1` 必須仍要求 OrgMaster app role。contract 欄位與 ACL 不變，因此既有 v1 schema manifest hash 保持；語意變更與 compatibility evidence 記錄於此 DEV。
- 任何 sibling core、共享登入資料庫 writer、新 IAM／Secret、正式資料修改或 Cloud Run 變更均不屬本機 RD／QC；Production release／L4 另依 owner gate 與明確授權執行。

## 驗收規劃

`O01` role-neutral active principal、AI-PDM-only admission及 OrgMaster session隔離；`O02` managed bind／legacy publish雙向競態與永久pair reservation；`O03` 同版authority＋grants；`O04` switch／revoke race；`O05` consumer僅讀明列版本化contract；`O06` v1欄位／ACL與雙版SSO parser回復；`O07` authority-independent Portal入口、委派、assignment version及scope mapping。2026-09-23 producer QC結果：`O01 PASS`（D57-01／02）；`O02 PASS`（D57-05／06）；`O03 PASS`（D57-03）；`O04 NOT_RUN`；`O05 NOT_RUN`；`O06 PARTIAL`（producer三個view的欄位／owner／ACL前後相同，OrgMaster session仍以app role隔離；跨repo v1／v2 parser recovery尚未跑）；`O07 PARTIAL`（直接／委派入口、assignment version及visibility不替代target allow已通過D57-02～04；AI-PDM company/resource mapping、`current`／`company-jenfu`完整normal-entry驗收待AIPDM/DEV-121）。證據為Platform工作樹 `.task-dev014/orgmaster/dev057-postgres-qc-r2.json`，task-owned PostgreSQL 18.4、6／6 checks、cleanup全PASS、Production writes=false。

## Architecture Closure Review：owner 可直接執行的邊界

審查基線 HEAD `c8a2e42e211be16142d9c60c3abc866361237003`（工作樹已有其他未提交文件，不是 release source lock）。原始 migration 013 的 shared active-principal view 把 OrgMaster-role 條件放入 legacy 身分分支；migration 014 另有 OrgMaster 專用 session admission；migration 020 的 Portal AI-PDM visibility 又只從 OrgMaster effective grants 生成。migration 021 已修正這三項 owner 邊界；D57-06 已在隔離 PostgreSQL 對 bind-first 與 publish-first 兩種順序實測鎖等待及衝突拒絕，不形成雙 mapping。

| 實作入口（repo-relative） | 本 DEV 固定工作 |
| --- | --- |
| `server/orgmasterSsoHandoff.ts`、`server/orgmasterSsoHandoff.test.ts`、`contracts/jenfu-sso-handoff/v2/` | target-first v1／v2 精確 parser。v1 assignmentVersion 僅驗格式，不與 mappingVersion 比；v2 無 `authorization`。保留 exact issuer／audience、source expiry、epoch、一次性 code 與 local session 上限。 |
| `server/orgmasterManagedIdentityRepository.ts`、`server/orgmasterManagedIdentityStore.ts`、`server/orgmasterPrincipalAdmissionRepository.ts`、`server/orgmasterPersistenceRepository.ts` 及既有 tests；migration 013／017 **唯讀參照** | managed bind／verify 與 legacy governance publish 共鎖 `managed_identity_admission_authority` singleton row；publisher 對 active proposed `identityLinks` 驗證 duplicate／managed collision／reservation owner，並原子新增 legacy reservations。任一順序的併發測試都不得提交雙 mapping。 |
| `server/applicationRoleAssignmentStore.ts`、`server/employeeAuthoritySwitchStore.ts`、相關 tests；`db/migrations/013_dev049_existing_google_primary_account_link.sql`、`014_dev050_orgmaster_session_admission.sql`、`020_dev014_current_projection_contract.sql` **只讀參照** | 驗 authority switch 的 CAS、row／outbox 同 commit、發佈後 grant 與 authorityVersion 同版；已套用 migrations 不可修改。 |
| `db/migrations/021_dev057_identity_grant_writer_fence.sql`、`scripts/lib/dev040-routine-release.mjs`、`config/release/dev040-orgmaster-independent-production-v3.json`、contract／PostgreSQL tests | 同一 forward-only migration 更新 producer writer fence與兩個 v1 view：active-principal legacy 分支移除 OrgMaster-role `EXISTS`；Portal AI-PDM visibility 從與 effective-grant 同等的有效 published 直接／委派 app 指派投影，移除 authority join。保留 active employee、link／assignment 有效期、lifecycle barrier、position／catalog／subject／scope、governance `versionNumber`→`assignment_version`、輸出欄位、`UNION ALL`、owner／SELECT ACL。更新完整 migration bundle 與 source-bound owner release gate；不改 OrgMaster 專用 session view 或其他 app 資料。 |

### Producer 修正的進入與停止條件

在 task-owned PostgreSQL 套用歷史 001–020 後，migration 021 先用固定鎖順序鎖 admission／persistence authority，再以**修正後候選投影**檢查每個 `(issuer, subject)` 的 active row count、principal／employee 對應與 legacy＋managed 交集；若既有 pair 歧義即 rollback。此後 managed bind／verify 與 governance publish 共用 admission row lock，publisher 在該 lock 內驗證 proposed active version、重複 pair、managed reservation 與 owner 漂移，再與治理 artifact 同 transaction 寫入 append-only reservations。競態 fixture 要雙向測試：先 bind 後 publish、先 publish 後 bind，各自最多一方成功且無中間雙列。

Portal visibility 的 AI-PDM 分支須從 active governance 的直接／有效委派、position 與 catalog-valid app 指派取有效入口，join active principal，遵守 employee／principal subject、有效期及 allowed scope；`authority_source` 不是入口條件，也不得直接從 local ACL 造入口。`assignment_version` 與同一 governance versionNumber 綁定，不能取 authorityVersion 或 mappingVersion。成功後讀回兩 view definition、欄位／ACL、OrgMaster session view 與 AI-PDM authority，證明 AI-PDM-only 角色不被 OrgMaster session 接受、legacy／OrgMaster 兩種 authority 有相同入口判定、無指派者不可見、target deny 不因 Portal 可見而被跳過。另讀回 scope key，確保 consumer 的 owner mapping 涵蓋有效發布值；未知值由 owner 處理 contract/data drift，不放寬為任意 scope。正式 Production migration 僅能經 owner-native bundle／manifest 與當時適用 authorization；未知 outcome 先讀 ledger／view 再判 replay，不做 down migration或人工 SQL。

精確不變條件：producer 對 `(issuer, subject)` 的 active mapping 正常結果恰一筆；`UNION ALL` 保留所有衝突列，consumer 使用上限兩筆查詢並將零筆、歧義、查詢失敗分開。`(employee, ai-pdm)` authority 必須恰一個；grant 只在 `orgmaster_authority` 有效，所有有效列的 employee／application／principal／authorityVersion 與同快照 authority row 一致。切換寫入及 outbox 同一 DB commit，未知結果以 durable operation receipt readback 再決定 replay；不得改成分次提交或在 outbox 未消費時回退舊權威。consumer 的唯讀 transaction 保護跨 view 的讀取一致性，不能補救 producer 自身分次 commit。

本輪已在 task-owned PostgreSQL 18.4 執行 migration 001–021及 synthetic fixtures：D57-01前置歧義拒絕、D57-02 role-neutral mapping／OrgMaster session deny／Portal入口與v1 surface相容、D57-03 authority 4與grant同版、D57-04 delegated recipient可見但沒有target grant、D57-05 historical pair reassignment與duplicate pair拒絕、D57-06雙向writer競態。證據manifest為 `C:\VIBE CODING\Jenfu-Platform\.task-dev014\orgmaster\dev057-postgres-qc-r2.json`；6／6 PASS、cleanup五項全true、`productionWrites=false`。另外 contract QC、DB boundary與57個 owner-release／migration tests PASS。未涵蓋O04 authority switch/revoke race、O05跨consumer ACL/read audit、O06跨repo parser rollback及O07 consumer scope mapping/browser；由AIPDM/DEV-121與JENFU/DEV-015剩餘整合驗收承接。Production build／release與L4目前不在本機 RD/QC 階段。

## 2026-09-24 local recheck

在 task-owned PostgreSQL 18.4 再執行 `npm run qc:dev-057:postgres`：D57-01～D57-08 全部 PASS，`executedCaseCount=8`，包括未分類 legacy mapping 不進 accounts adapter、managed mapping 僅經 exact identity/admission match 進入 accounts adapter，以及 Platform legacy projection 保持不變。client／auxiliary clients 已關閉、cluster 已停止、port 已釋放、temporary root 已移除，`productionWrites=false`。Authority runner unit tests另驗證 canonical mapping 存在但 typed accounts view 缺列時，CAS 前即 fail closed。Platform 隔離 PostgreSQL 驗證其 owner-owned CAS 僅接受 `human_personal`，相同 operation 可重播且不重複 receipt/outbox，runtime 無 execute 權限。這些是本地契約證據，不升格為 Production readback、authority switch、browser 或 L4 evidence；Production 必須先讀回兩個 OrgMaster views 一致後才可考慮切換。

## 2026-09-25 AI-PDM v4 catalog consumer cutover

AI-PDM 為 principal-first DEV-087 明列四項先前缺漏的 workspace／review-submit 能力，已產生 `ai-pdm.role-catalog.2026-09-25.v4`。OrgMaster 以 hash-pinned `config/catalogs/ai-pdm-role-catalog.v4.json` 作為**新指派**的目前 catalog；原 `contracts/jenfu-platform-entitlement/v1/fixtures/application-role-catalog.sample.json` 與 manifest 保持凍結，只驗證已存在 v3 assignment 的角色身份、名稱及 scope provenance。有效權限仍依 AI-PDM active catalog 及本 owner 已發布 authority／assignment 決定，歷史 v3 fixture 不成為第二個 runtime 權限來源。一般 V3 UPSERT 指派／代理若帶 v3 版本直接拒絕，REVOKE 與未改的歷史指派可維持；system_admin 的專用操作、subject 與 AAL2 規則不因升版放寬。

本機 catalog／assignment 聚焦測試與 typecheck PASS，`check:db-boundary` PASS；v4 artifact 尚未與 AI-PDM migration 066、OrgMaster 正式 release 與 Production readback 完成排序，故目前沒有發布或改動現有指派。切流前須以實際 OrgMaster draft／published 指派快照驗證 v3 歷史列仍有效、新 v4 寫入要求生效，且 producer view 的 version／hash 與本 artifact 完全相同；失敗不得以重寫既有 assignment 或回退 Firebase UID bridge 補救。

**Catalog owner release order（2026-09-25 修訂）。** AI-PDM owner 先由 source-frozen migration runner 在不改 service traffic 的獨立 migration 執行套用 065／066，將 v4 發布到 `ai_pdm_contract.v_application_role_catalog_v1` 並讀回九個 role 的 version／catalog hash／definition hash；OrgMaster 才發布 v4 consumer，最後 AI-PDM 將 v4 runtime 切流。這避免新 AI-PDM UI 在 OrgMaster 尚為 v3 時短暫失效，也不建立永久雙目錄 runtime。OrgMaster Cloud SQL catalog registry 在提供治理文件或接受寫入前，直接查 producer view，要求其與已審查 v4 artifact 完全一致；正式 view 仍是唯一 published catalog，內建 JSON 只是 consumer 驗證基準。v3 只允許作既有 assignment provenance 與回復舊 revision 的資料，新的 preview／UPSERT 不接受 v3。等 principal-first 全部切流與回復觀察完成後，移除 v1／v2 寫入轉接與歷史 catalog runtime 選擇；歷史 assignment、audit 與原始 migration 不刪除。
