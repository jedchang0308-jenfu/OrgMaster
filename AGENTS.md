## Shared database boundary — DEV010_DB_RULESET_V1

- DEV-010 shared PostgreSQL DDL must be delivered as a forward-only migration under `db/migrations`; never edit the shared managed database by hand.
- This repository may create or alter only `orgmaster_core` and `orgmaster_contract`. Never reference another application's `*_core` schema.
- Cross-application access must use a versioned `*_contract` object. A breaking contract change requires a new version and a compatibility window.
- Never modify or delete an applied migration. Correct it with a new migration.
- Do not create application-owned objects in `public`.
- Runtime identities must never receive owner, DDL, or migrator privileges.
- Before completing a database change, run `npm run check:db-boundary`.
- If schema ownership or contract impact is unclear, stop and record the decision in the relevant DEV or ADR before changing SQL.

## OrgMaster continuous production release — DEV040_R2_RELEASE_V3_DIRECT_RUN_APP

- OrgMaster owns only `orgmaster-prod`, `orgmaster_core`, `orgmaster_contract`, its app-specific release bucket/repository/state, entry policy, and release identities. It must not deploy or read sibling source, mutate sibling traffic, own shared foundation state, or create Cloud SQL, DNS, Hosting, or sibling Secret resources. V3 production is deployed; current release authority is DEV-040 section 35 and QA section 13 (2026-09-16 run `35070877802`, source `5e3516737f7fc98cf0e2a1a30bdb2852a0817b0e`, terminal `RELEASED`), not historical S1C/NOT_RUN text.
- Production release uses one immutable `releaseCapsuleRef`, service-wide concurrency `production-release-orgmaster-prod`, and exact ten-stage flow `prepare -> build -> migrate -> candidate -> entrypoint -> verify -> decision -> activate -> canonical -> finalize`. The app-owned `entrypoint` stage may patch only `ingress,defaultUriDisabled,invokerIamDisabled` with fresh etag and zero template/traffic drift. Recovery is own traffic rollback, tag cleanup, then entry-baseline restore. No in-run human acknowledgement or fixed dwell is allowed; local evidence has `releaseAuthority=false`.
- The production canonical entry is the provider-verified `orgmaster-prod` Cloud Run `run.app` default URL. Runtime candidate origin is one exact full origin injected into the revision; wildcard and guessed hash-host authorization are forbidden. Candidate/entrypoint/verify readback may match exact tag origins projected from this same service's provider-returned URI/URLs; this does not add runtime allowed origins. Do not use `org.jenfu.com.tw`, Firebase Hosting, or the shared Load Balancer as current serving. Existing edge assets are `RETAINED_UNUSED_EDGE`; do not delete or unlink Billing in this DEV.
- The production database target is exactly `jenfu-platform-prod / asia-east1 / jenfu-platform-prod-pg / jenfu_prod`. Runtime and migrator IAM DB logins are distinct. The staging N1C profile remains staging-only and must never be reused as production authority.
- DEV-040 R2 keeps the exact transformed N1C 001–010 ledger as the immutable baseline. The historical DEV-014 source profile is the exact 001–020 bundle; the current DEV-057 controlled source profile appends 021–026 for principal-first producer release, including the forward-only 026 session-policy correction; ordinary releases require that full bundle unchanged and execute zero DDL. The historical DEV-013 production activation recovery slice appended exact repository-owned 012／013／014／015 after preserving the 001–011 prefix byte-for-byte. DEV-014 controlled remediations append only exact forward migrations: 016 lifecycle producer contract, 017 application registration, 018 Employee activation routine, 019 canonical workspace revision, and 020 current contract projection. Migration 020 may replace only the three named `orgmaster_contract` read-only views and must not alter the historical `access_governance` compatibility layer, columns, grants, authority rows, assignments, or Employee data. Every controlled append must run the owner migration Job before candidate creation and publish a source-bound migration receipt. Missing, reordered, changed, or cross-mode entries fail closed. Account enrollment and external side effects stay disabled unless a separate production authorization receipt exists.
- Before accepting a release change, run `npm run test:dev-040:r2`, `npm run qc:dev-040:r2`, `npm run test:dev-040:abort`, `npm run check:db-boundary`, `npm test -- --testTimeout=30000`, and `npm run build`.
- Application releases use one normal path: `npm run deploy:production` (read-only preflight: `npm run deploy:production -- --check`). DEV-040 sections 35–39 require an immutable prior RELEASED baseline, unchanged infrastructure inputs, and all ten protected stages. Ordinary releases require `migrate=UNCHANGED_VERIFIED` with zero DDL/import/bootstrap. Sealed DEV-014 projection remediation may execute only exact migration 020 from the exact 001–019 baseline and requires a source-matched `APP_INFRA_IMAGE_ROTATION` receipt for the migration-runner digest. During a controlled append, the infrastructure fingerprint may exclude only `infra/google-cloud/dev-040-production-release/migration-runner.Dockerfile`; the source-matched receipt and complete-set Terraform gate remain mandatory, while every other infrastructure path stays immutable. The owner migration Job must finish and its receipt must validate before candidate creation. Baseline is evidence, not an optional bootstrap bypass; missing/changed prerequisites never fall back to initialization. The schema runner rejects data/bootstrap arguments and requires the existing 001–010 ledger before applying an authorized pending suffix through 020. Historical receipts remain readable for recovery; historical initialization code is not a release dependency. Do not refresh unchanged foundation evidence merely because application source changed or inspect sibling source. Within one human-authorized project/action/environment scope, do not require repeated approval for each release step. Git archives include committed files only; unrelated untracked files are preserved and excluded, while tracked dirt still blocks source freeze.
- The bounded DEV-014 login-fixture correction may reuse an earlier exact `APP_INFRA_IMAGE_ROTATION` receipt only when the current service source and that receipt's Git source revision have identical complete ordinary infrastructure fingerprints, runtime and the current 001–020 migration bundle are unchanged, and prepare revalidates the sealed receipt. This exception is fixed to `ai-pdm` and Employee IDs `01a0c82b-11c6-77ab-887f-58df9d243e63`／`01a0c82b-372c-7d20-ba3b-6e3b892d2f63`; it does not authorize another Employee, application, schema, IAM, Secret, service, or traffic scope.
- Automated SBOM export may list bucket metadata and attach Container Analysis occurrences, but object authority must be conditioned to the encoded `orgmaster-release` prefix in the existing regional Artifact Analysis bucket; never grant project-wide Storage Admin/Object Admin or a sibling prefix. These bindings are `incident_runtime_enabled` APP_INFRA_B resources so an already-bootstrapped B state can add them without an unsafe A rollback plan. Migration overrides require `roles/run.jobsExecutorWithOverrides` only on `orgmaster-prod-migration-runner` for `orgmaster-prod-deployer`; preserve the older exact-job `roles/run.invoker` as a non-broadening additive compatibility binding so Terraform plans remain create/no-op/read.
